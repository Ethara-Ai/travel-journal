# ==============================================================================
# TRAVEL JOURNAL - ENVIRONMENT VARIABLES
# ==============================================================================
# This file serves as a template for your local .env file.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the same directory: `cp .env.example .env`
# 2. Fill in the required values for your environment
# 3. Restart the development server after making changes
#
# IMPORTANT NOTES:
# - Never commit your actual .env file to version control
# - All Vite environment variables MUST be prefixed with VITE_ to be exposed
#   to the client-side code
# - Variables without VITE_ prefix will only be available in server-side code
# ==============================================================================


# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================

# VITE_APP_TITLE
# --------------
# The display name of your application.
# This will be used in the browser tab title, headers, and metadata.
# Type: String
# Required: No (defaults to "Travel Journal" if not set)
# Example: "My Travel Adventures"
VITE_APP_TITLE=Travel Journal

# VITE_APP_VERSION
# ----------------
# The current version of your application.
# Useful for cache busting, debugging, and displaying version info to users.
# Follows semantic versioning (MAJOR.MINOR.PATCH).
# Type: String
# Required: No
# Example: "2.1.0"
VITE_APP_VERSION=1.0.0


# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================

# VITE_PORT
# ---------
# The port number for the development server.
# This should match the port configured in vite.config.js.
# Type: Number
# Required: No (defaults to 5173)
# Example: 3000
VITE_PORT=5173


# ==============================================================================
# FEATURE FLAGS
# ==============================================================================
# These flags enable or disable specific features in the application.
# Useful for gradual rollouts, A/B testing, or environment-specific behavior.

# VITE_ENABLE_ANALYTICS
# ---------------------
# Enables or disables analytics tracking throughout the application.
# Set to "true" to enable tracking, "false" to disable.
# Type: Boolean (as string: "true" or "false")
# Required: No (defaults to false)
# Example: "true"
# VITE_ENABLE_ANALYTICS=false

# VITE_DEBUG_MODE
# ---------------
# Enables verbose logging and debugging features.
# Should be "false" in production environments.
# When enabled, additional console logs and debug panels may be visible.
# Type: Boolean (as string: "true" or "false")
# Required: No (defaults to false)
# Example: "true"
# VITE_DEBUG_MODE=false


# ==============================================================================
# API CONFIGURATION
# ==============================================================================
# Settings for connecting to backend services and external APIs.

# VITE_API_URL
# ------------
# The base URL for the backend API server.
# Include the protocol (http/https) and any base path.
# Do NOT include a trailing slash.
# Type: URL String
# Required: No (only if using backend features)
# Development: "http://localhost:5173/api"
# Production: "https://api.yourdomain.com/v1"
# VITE_API_URL=http://localhost:5173/api

# VITE_IMAGE_HOST_URL
# -------------------
# Base URL for hosted images (CDN or image hosting service).
# Used for optimizing image loading and serving from edge locations.
# Do NOT include a trailing slash.
# Type: URL String
# Required: No (only if using external image hosting)
# Example: "https://cdn.yourdomain.com/images"
# VITE_IMAGE_HOST_URL=https://your-image-host.com


# ==============================================================================
# THIRD-PARTY SERVICE API KEYS
# ==============================================================================
# API keys for external services. Keep these secret and never commit real values.
#
# ⚠️  SECURITY WARNING: These keys will be exposed in the client-side code.
# Only use keys with appropriate client-side restrictions configured.

# VITE_GOOGLE_MAPS_API_KEY
# ------------------------
# API key for Google Maps JavaScript API.
# Used for displaying interactive maps and location features.
# Get your key: https://console.cloud.google.com/google/maps-apis
# Type: String
# Required: No (only if using map features)
# Security: Restrict key to your domains in Google Cloud Console
# Example: "AIzaSyB1234567890abcdefghijklmnopqrstuvwx"
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# VITE_UNSPLASH_API_KEY
# ---------------------
# API key for Unsplash image service.
# Used for fetching high-quality stock photos for travel destinations.
# Get your key: https://unsplash.com/developers
# Type: String
# Required: No (only if using Unsplash integration)
# Rate Limits: 50 requests/hour for demo apps, 5000/hour for production
# Example: "your-unsplash-access-key-here"
# VITE_UNSPLASH_API_KEY=your_unsplash_api_key_here


# ==============================================================================
# ADDITIONAL NOTES
# ==============================================================================
#
# ENVIRONMENT-SPECIFIC FILES:
# Vite supports environment-specific files that override this base config:
#   - .env.local          → Local overrides (ignored by git)
#   - .env.development    → Development environment
#   - .env.production     → Production environment
#   - .env.test           → Test environment
#
# LOADING PRIORITY (highest to lowest):
#   1. .env.[mode].local
#   2. .env.[mode]
#   3. .env.local
#   4. .env
#
# ACCESSING VARIABLES IN CODE:
#   const title = import.meta.env.VITE_APP_TITLE;
#
# For more information, see: https://vitejs.dev/guide/env-and-mode.html
# ==============================================================================
